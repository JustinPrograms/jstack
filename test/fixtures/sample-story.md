# DEMO-101: Remember a local display preference

Add an opt-in preference that keeps the sample application's compact display choice between local sessions.

Acceptance notes:

- The default remains unchanged for existing users.
- Invalid stored values fall back safely.
- Unit tests cover the default, saved value, and invalid value.

This fixture is invented and contains no external ticket text, names, repositories, or URLs.
